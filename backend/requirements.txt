# NOTE: torch is installed CPU-only via the extra index URL in
# render-build.sh / the Render build command -- see README/deployment
# instructions. Do NOT change this line to "torch>=2.3" without also
# pointing pip at the CPU wheel index, or Render's free-tier build will
# try to pull the full CUDA build (multiple GB) and fail/exceed limits.
torch>=2.3
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
opencv-python-headless>=4.9
numpy>=1.26
pandas>=2.2
pydantic>=2.6
ultralytics>=8.3
